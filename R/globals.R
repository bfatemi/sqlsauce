# For variables used in "data.table" in ColumnInfo function
globalVariables(c("B", "Column", ":=", "IsKey", ".", ".N", "Table", "Type"))

# For variables used in "data.table" in TableInfo function
globalVariables(c(".", ".N", "Database"))

# For variables used in "data.table" in SeeConn function
globalVariables(c("Names", "Values", ":="))

# For variables used in "data.table" in Timer function
globalVariables(c("Names", "Values", ":="))

# For variables used in "data.table" in WhereSauce function
globalVariables(c("querytext"))

# For variables used in "data.table" in WhereSauce function
globalVariables(c("V1", "ErrorMessage", "Description"))
